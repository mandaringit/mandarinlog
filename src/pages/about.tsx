import React, { memo } from 'react';
import Layout from '../components/Layout';

const About = memo(() => {
  return (
    <Layout>
      <div>
        <h1>안녕하세요!</h1>
      </div>
    </Layout>
  );
});

export default About;
